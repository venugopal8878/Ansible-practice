-name: install nginx
 hosts: local
 connection: local
 #become: yes # run with sudo access
 tasks: 
  -name: install nginx
   ansible.builtin.package:
    name: nginx
    state: present
  -name: service start
   ansible .builtin.services:
    name: nginx
    state: started