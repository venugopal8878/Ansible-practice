- name: 
  hosts: web
  #connection : local
  become: yes # run with sudo access
  tasks: 
   - name: install nginx
     ansible.builtin.package:
      name: nginx
      state: present
   - name:
     ansible.builtin.service:
      name: nginx
      state: stopped
      enabled: yes

